kind: pipeline
type: kubernetes
name: feature

steps:
  - name: Run unit tests
    image: golang:1.16
    commands:
      - go mod download
      - go test ./...
    when:
      branch:
        - master
      event:
        - pull_request