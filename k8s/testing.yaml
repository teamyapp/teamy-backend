apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  namespace: testing
  name: teamy
spec:
  backendType: vault
  vaultMountPoint: kubernetes
  vaultRole: secrets-read-role
  data:
    - name: user
      key: kv/data/secrets/db/postgres/teamy/testing
      property: user
    - name: password
      key: kv/data/secrets/db/postgres/teamy/testing
      property: password
    - name: db-name
      key: kv/data/secrets/db/postgres/teamy/testing
      property: db-name
---
apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  namespace: testing
  name: apps
spec:
  backendType: vault
  vaultMountPoint: kubernetes
  vaultRole: secrets-read-role
  data:
    - name: github-webhook-secret
      key: kv/data/secrets/testing/apps
      property: github-webhook-secret
---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: testing
  name: teamy-backend
  labels:
    app: teamy-backend
  annotations:
    # force policy will ensure that deployment is updated
    # even when tag is unchanged (latest remains)
    keel.sh/policy: force
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: teamy-backend
  template:
    metadata:
      labels:
        app: teamy-backend
    spec:
      containers:
        - name: teamy-backend
          image: registry.digitalocean.com/teamyapp/teamy-backend-testing:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 9000
            - containerPort: 9001
          env:
            - name: DB_HOST
              value: postgres.db.svc.cluster.local
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: teamy
                  key: user
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: teamy
                  key: password
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: teamy
                  key: db-name
            - name: IDENTITY_API_ENDPOINT
              value: http://cloud.testing.svc.cluster.local/identity
            - name: CLOUD_API_HOST
              value: cloud
            - name: CLOUD_API_PORT
              value: "81"
            - name: CLOUD_API_SHOULD_ENCRYPT
              value: "false"
            - name: SERVICE_RUNNER_WEB_SERVER_PORT
              value: "9000"
            - name: SERVICE_RUNNER_GRPC_SERVER_PORT
              value: "9001"
            - name: APPS_GITHUB_APP_NAME
              value: teamy-apps-testing
            - name: APPS_GITHUB_WEBHOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: apps
                  key: github-webhook-secret
---
apiVersion: v1
kind: Service
metadata:
  namespace: testing
  name: teamy-backend
spec:
  selector:
    app: teamy-backend
  ports:
    - name: web-services
      port: 9000
    - name: rpc-services
      port: 9001
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  namespace: testing
  name: teamy-backend
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`web-backend.testing.teamyapp.com`)
      services:
        - kind: Service
          name: teamy-backend
          namespace: testing
          port: 9000
          passHostHeader: true
    - kind: Rule
      match: Host(`rpc-backend.testing.teamyapp.com`)
      services:
        - kind: Service
          name: teamy-backend
          namespace: production
          port: 9001
          passHostHeader: true
          scheme: h2c
  tls:
    secretName: teamy-cert

